// Placeholder - real implementation is WBS item 13 (seção 6.3/6.4): contagem agregada por fase + gate
// pendente com deep-link, consumindo GET /workspaces/{id}/dashboard.
export function DashboardPage() {
  return (
    <section>
      <h1>Dashboard</h1>
      <p>Em construção.</p>
    </section>
  );
}
